import styled from "styled-components";

const Container = styled.div`

`

export default function ServiceMail () {
    return(
        <Container>
            메일 모달창 형식으로 보여주기
        </Container>
    );
}